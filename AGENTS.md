# WinBox agent 工作规则

2026-09-22：用户完成一系列人工测试，确认前后端迁移基本完成并关闭重构阶段。当前开发版本 `3.0.0-alpha.3`；不再执行历史迁移台账。开发入口见 [架构](docs/architecture.md)，按改动阅读 [前端设计](docs/frontend.md)、[后端规范与契约](docs/backend.md)、[验证与发布](docs/development.md)。

## 边界

- React + 微软 Fluent UI v9；Tauri 2 + Rust + 独立 sing-box。只支持 Windows AMD64/x64，发行 NSIS + 官方 updater；不恢复 Vue/Wails/Go、portable/MSI，不构建 ARM64。Linux 属于后续版本，仅保留合理的字体/材质回退。
- 先核对工作区、源码及调用链，保留他人未提交修改。一般修复、等价优化和已授权清理自行推进；实质产品重设计、发行形式或权限行为变化需确认。
- WinUI 3 规范优先，Fluent 官方控件承担交互/焦点/键盘。优先 token、公开 props/slots/motion；仅为产品布局和特有效果补最小样式。保留现有功能、操作路径和必要反馈；无必要或不符合规范的样式可清理并说明影响。
- Windows API、注册表、任务计划和系统进程操作留在 Rust platform 边界；前端不获得任意 shell/fs/远程内容的高权限入口。

## 实现与交接

1. 顺序：是否需要 → 复用代码 → 标准库/系统原生 → Tauri 官方/已有依赖 → 成熟组件 → 最小自定义。不开空 Linux 实现、DI/插件框架、单实现无用 trait 或通用 UI 代理库。
2. 契约变化同步 Rust、TypeScript、全部调用者、测试和相关指南。运行态以后端为准；初始化快照不得覆盖更新的事件，命令成功不能猜测内核状态。
3. 新依赖记录准确版本、官方来源、许可证、维护状况、用途及限制，提交锁文件。已知临时简化用 `ponytail:` 注释写明边界和替换条件。
4. 非平凡逻辑保留最小可运行回归；UI 改动检查真实渲染/交互/中间帧，编译不等于视觉验收。生成截图、视频、构建产物不入库。
5. 更新当前规范而非堆叠阶段日志；历史从 Git 查阅。报告完成、验证、限制；不把未执行的矩阵说成通过。
6. 推送/PR/发布遵从用户授权。本次只推送 `dev`，维护者手动 PR 到 `main`，现有工作流负责发布；不自行合并或发 Release。
